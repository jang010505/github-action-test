name: step02_condition
on: workflow_dispatch
jobs:
  build:
    if: github.repository == 'jang010505/github-action-test'
    runs-on: ubuntu-latest
    steps:
    - name: Print Result
      run: |
        echo "Run Build on jang010505/github-action-test"

